<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="ctec.stateapp.controller.BackPage">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="New Button"
        android:id="@+id/SubmitButton"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_marginRight="84dp"
        android:layout_marginEnd="84dp"
        android:layout_marginTop="110dp" />

    <CheckBox
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="New CheckBox"
        android:id="@+id/isTired"
        android:layout_below="@+id/SubmitButton"
        android:layout_alignRight="@+id/SubmitButton"
        android:layout_alignEnd="@+id/SubmitButton"
        android:layout_marginTop="36dp"
        android:hint="Are you tired?" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/NameText"
        android:layout_below="@+id/isTired"
        android:layout_toStartOf="@+id/SubmitButton"
        android:hint="Type in your name please"
        android:layout_alignRight="@+id/AgeText"
        android:layout_alignEnd="@+id/AgeText" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="number"
        android:ems="10"
        android:id="@+id/AgeText"
        android:layout_below="@+id/NameText"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="34dp"
        android:hint="Type in your age please" />

</RelativeLayout>
